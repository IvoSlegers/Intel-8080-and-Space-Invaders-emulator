#include "ui.hpp"

#include "application.hpp"

namespace emulator
{
    UI::UI(): window(sf::VideoMode(800, 600), "intel 8080 emulator - Space invaders")
    {}

    void UI::run(Application& parentApplication)
    {
        
    }

    void UI::draw()
    {

    }
} // namespace emulator
